server:
  port: 8081

  tomcat:
    uri-encoding: UTF-8
    threads:
      max: 1000
      min-spare: 30

  servlet:
    context-path: /

spring:
  datasource:
    name: test
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test
    username: root
    password: 123123

logging:
  level:
    com: debug

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.ssmdemo.model

jwt:
  header: Authorization
  base64-secret: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9
  token-validity-in-seconds: 3600
